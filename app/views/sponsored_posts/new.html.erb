<h1>New Post</h1>

<div class="row">
  <div class="col-md-4">

    <p>Thank you for purchasing a sponsored ad with Bloccit!</p>
    <ul>
      <li>Make sure it's an offer they can't refuse</li>
      <li>Include great imagery.</li>
      <li>Remember— the customer is alright right, sometimes.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= form_for [@topic, @spons_post] do |f| %>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
    </div>
    <div class="form-group">
         <%= f.label :price %>
         <%= f.number_field :price, class: 'form-control', placeholder: "Enter Promotion Price" %>
       </div>
    <div class="form-group">
      <%= f.label :body %>
      <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post body" %>
    </div>
    <div class="form-group">
      <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
    <% end %>
  </div>
</div>

<h1><%= @spons_post.title " - #{@spons_post.price}"%></h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @spons_post.body %></p>
   </div>
   <div class="col-md-4">
 <!-- #5 -->
     <%= link_to "Edit", edit_topic_post_path(@spons_post.topic, @spons_post), class: 'btn btn-success' %>
     <%= link_to "Delete Post", [@spons_post.topic, @spons_post], method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this post?' } %>
   </div>
 </div>
